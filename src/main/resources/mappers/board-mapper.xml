<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">

	<select id="selectAll" resultType="com.study.app.domains.board.BoardDTO">
		SELECT *
		FROM board
		ORDER BY seq DESC
	</select>

	<update id="updateViewCount">
		UPDATE board
		SET view_count = view_count + 1
		WHERE seq = #{seq}
	</update>

	<select id="selectBySeq" resultType="com.study.app.domains.board.BoardDTO">
		SELECT *
		FROM board
		WHERE seq = #{seq}
	</select>

	<insert id="insert">
		INSERT INTO board (seq, writer, title, contents, view_count, write_date)
		VALUES (board_seq.NEXTVAL, #{writer}, #{title}, #{contents}, 0, sysdate)
	</insert>

	<update id="update">
		UPDATE board
		SET title = #{title},
		contents = #{contents},
		writer = #{writer}
		WHERE seq = #{seq}
	</update>


	<delete id="delete">
		DELETE FROM board WHERE seq = #{seq}
	</delete>


</mapper>